onfigure Nginx on a new Ubuntu machine with custom HTTP response header

# Install Nginx
sudo apt-get update
sudo apt-get install -y nginx

# Define the custom HTTP response header
HEADER="\\\n\tadd_header X-Served-By \$hostname;\n"

# Add the custom header to the Nginx default configuration
sudo sed -i "23i $HEADER" /etc/nginx/sites-available/default

# Restart Nginx to apply the changes
sudo systemctl restart nginx
